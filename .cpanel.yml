---
deployment:
  tasks:
    # Diagnostic marker: proves cPanel actually executed this deployment YAML.
    - /bin/bash -lc 'date -u > "$HOME/lien-phat-cpanel-trigger.txt"; git rev-parse HEAD >> "$HOME/lien-phat-cpanel-trigger.txt"'
    # Deploy the already-built package outside the managed Git checkout.
    - /bin/bash scripts/cpanel-deploy.sh

---
deployment:
  tasks:
    # The Git checkout remains the source repository.
    # This task deploys the already-built package to ~/lien-phat-ai-production.
    - /bin/bash scripts/cpanel-deploy.sh

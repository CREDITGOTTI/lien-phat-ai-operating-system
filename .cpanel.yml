---
deployment:
  tasks:
    # cPanel Git Version Control runs this from the repository.
    # The script detects Node, installs dependencies, builds the Nitro server,
    # verifies the output, writes deployment.log, and restarts Passenger.
    - /bin/bash scripts/cpanel-deploy.sh
